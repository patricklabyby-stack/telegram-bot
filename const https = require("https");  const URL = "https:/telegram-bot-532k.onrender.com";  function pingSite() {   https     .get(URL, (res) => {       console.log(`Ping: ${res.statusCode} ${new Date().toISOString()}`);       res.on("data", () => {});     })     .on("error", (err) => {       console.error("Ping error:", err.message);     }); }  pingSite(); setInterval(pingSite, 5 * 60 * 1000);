name: Ping Bot

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -s https://telegram-bot-532k.onrender.com > /dev/null
